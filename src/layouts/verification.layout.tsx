
import React from 'react';
import {
    SafeAreaView,
} from 'react-native';
function VerificationLayout(): JSX.Element {
    return (
        <SafeAreaView >
        </SafeAreaView>
    );
}

export default VerificationLayout;
